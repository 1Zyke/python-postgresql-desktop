# python-postgresql-desktop

Practical example of python and postgresql, with a simple desktop application, using a Python module called: Tkinter. 
This module allows us to create some buttons and improve the interface. 
And PostgreSQL helps us to make a query request to save the data we insert in the entries inside PostgreSQL.
